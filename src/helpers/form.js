/* eslint-disable */
export const error = {
    required: "This field is required for filling",
    minLength: "Max length",
    maxLength: "Min length",
};

/* eslint-disable */
export const  formError=(input)=>{
    console.log(`input : ${input}`)
    // const   param = input.$params
    //         keys = Object.keys(param);

    // let errorList = [];

    // keys.forEach(key => {

    //     if(input[key] === true){
    //          errorList.push(error[key])
    //     }
       
        //console.log(`${key} : ${obj[key]}`);
    //});

   // return errorList;


} 